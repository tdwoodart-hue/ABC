rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function hasAllowedEmail() {
      return isSignedIn()
        && request.auth.token.email != null
        && request.auth.token.email in [
          'tdwoodart@gmail.com',
          'duong@gmail.com',
          'chucga@gmail.com'
        ];
    }

    // This is a private couple app.
    // Only the explicitly allowed accounts may read or write Firestore.
    match /{document=**} {
      allow read, write: if hasAllowedEmail();
    }
  }
}
